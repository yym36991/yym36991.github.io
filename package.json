{
  "name": "notes_site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run sync && vitepress dev docs --host 127.0.0.1",
    "build": "npm run sync && vitepress build docs",
    "preview": "vitepress preview docs --host 127.0.0.1",
    "sync": "node scripts/sync-notes.mjs",
    "git:auto-commit": "bash scripts/auto-commit-notes.sh"
  },
  "devDependencies": {
    "vitepress": "latest"
  }
}
