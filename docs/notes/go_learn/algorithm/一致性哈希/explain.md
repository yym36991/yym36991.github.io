


如果有2个collect,每个collect对应150个虚拟节点，一共300个虚拟节点。就使用这300个虚拟节点均匀分散在哈希环上，然后业务的mysql实例经过哈希之后，会对应在哈希环上的某个点假设是A，就把这个mysql分配给 距离A最近的虚拟节点对应的collect上。如果扩容到3个collect，就是450个虚拟节点，均匀分布在哈希环上，然后同理找到距离A点最近的某个collect对应的虚拟节点, 然后分配给这个collect?